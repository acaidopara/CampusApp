@inject NetworkState Network

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Network.InitializeAsync();
        }
    }
}
